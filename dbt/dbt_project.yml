name: nyc_taxi_lakehouse
version: "1.0.0"

profile: nyc_taxi_lakehouse

model-paths: ["models"]
macro-paths: ["macros"]
test-paths: ["tests"]

models:
  nyc_taxi_lakehouse:
    core:
      +materialized: table
      +table_type: hive
      +format: parquet
      +s3_data_dir: s3://nyc-taxi-lakehouse-tntk/gold/core/
    marts:
      +materialized: table
      +table_type: hive
      +format: parquet
      +s3_data_dir: s3://nyc-taxi-lakehouse-tntk/gold/marts/